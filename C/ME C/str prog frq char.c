#include<stdio.h>
#include<string.h>
main()
{
    int i,j,n;
    char s[100];
    printf("enter the string : ");
    gets(s);
    n=strlen(s);
    for(i=0;i>=97 && i<=122; i++)
    {
        c=0;
        for(j=0;j<n-1;j++)
        {
            if(i==s[j])
        }
    }
}
