#include<iostream>
using namespace std;
class Node
{
    int data;
    Node *next;
public:
    insert1(LL *head)
    {
        if(head==NULL)
            {
                p=new(Node);
                *head=p;
                p->next=NULL;
            }
            else
            {
                for
            }
    }

};
